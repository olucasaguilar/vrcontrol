<legend>Editar Tipo de Entidade</legend>

<%= form_with(model: @entity_type) do |f| %>

  <div class="form-group">
    <%= f.label 'Descrição', { class: "col-md-4 control-label"} %>
    <div class="col-md-4 inputGroupContainer">
      <div class="input-group">
        <%= f.text_field :nome, class: "form-control", placeholder: "Tipo de Entidade" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <label class="col-md-4 control-label"></label>
    <div class="col-md-4">
      <%= f.submit "Atualizar", class: "btn btn-primary" %>
      <%= link_to "Cancelar", entity_types_path, class: "btn btn-secondary" %>
    </div>
  </div>

<% end %>

<br>
<%= button_to 'Excluir', @entity_type, method: :delete, class: "btn btn-danger" %>

      